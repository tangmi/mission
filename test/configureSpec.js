var configure = require('../lib/configure');

var assert = require("assert");

describe('lib/configure', function() {
	describe('exports.queue', function() {
		it('should add items to the queue', function() {

		});
		it('should read the first item every time', function() {

		})
	})
})